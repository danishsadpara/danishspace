"use client";
import { Typography } from "@mui/material";
import React from "react";

const Register = () => {
  return (
    <div>
      Register page and also (auth) means that in url the auth will be omitted
      <Typography variant="caption">
        (actual url is as: 3000/auth/register)
      </Typography>
    </div>
  );
};

export default Register;
