import { makeStyles } from "@mui/styles";

export const styles = makeStyles(() => ({
  modal: {
    // width: "max-content",
  },
  main: {
    display: "flex",
    alignItems: "center",
    flexDirection: "row",
    gap: "5px",
  },
}));
